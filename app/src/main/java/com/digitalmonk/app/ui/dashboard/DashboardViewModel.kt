package com.digitalmonk.app.ui.dashboard

import androidx.lifecycle.ViewModel

// Ready to hold dashboard-specific state as the screen grows
// e.g. screen time stats, app usage data, filter status streams
class DashboardViewModel : ViewModel()